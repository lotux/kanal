<ion-view ng-controller="channelsCtrl">
    <ion-content overflow-scroll="true" padding="false" class="has-header">
        <h2>جستجوی ملک</h2>
        <form ng-submit="maskanSearch()">
            <div class="list">		
		

 <label class="item item-input">
                    <span class="input-label">نوع معامله</span>
                    <span class="input-label"></span> {{selectedMoameleType}}
                    <button class="button button-dark" modal-select="" ng-model="selectedMoameleType" options="moameleTypes" modal-title=نوع معامله را انتخاب نمایید">نوع معامله را انتخاب نمایید<div class="option">
                    {{option}}
                </div>
                    </button>
                </label>
		<label class="item item-input ">
                    <span class="input-label">شهر : </span>
                    <span class="input-label">تهران</span>
                </label>

	  <label class="item item-input">
                    <span class="input-label">نوع ملک</span>
                    <span class="input-label"></span> {{selectedMaskanType}}
                    <button class="button button-dark" modal-select="" ng-model="selectedMaskanType" options="maskanTypes" modal-title=نوع ملک را انتخاب نمایید">نوع ملک را انتخاب نمایید<div class="option">
                    {{option}}
                </div>
                    </button>
                </label>

                <label class="item item-input item-floating-label">
                    <span class="input-label">متراژ از</span>
                    <input type="text" placeholder="متراژ از" ng-model="channel.title" >
                </label>
                <label class="item item-input item-floating-label">
                    <span class="input-label">متراژ تا</span>
                    <input type="text" placeholder="متراژ تا" ng-model="channel.address" >
                </label>              

            </div>
            <div class="padding">
                <button type="submit" class="button button-block button-dark">ساخت کانال</button>
            </div>
        </form>
    </ion-content>
</ion-view>
