<ion-view ng-controller="channelsCtrl">
    <ion-content overflow-scroll="true" padding="false" class="has-header">
        <h2>جستجوی ملک</h2>
        <form ng-submit="maskanSearch()">
            <div class="list">		
                <label class="item item-input item-floating-label">
                    <span class="input-label">نام کالا</span>
                    <input type="text" placeholder="نام کالا" ng-model="goods.title" >
                </label>
		
		<label class="item item-input ">
                    <span class="input-label">شهر : </span>
                    <span class="input-label">تهران</span>
                </label>

                <label class="item item-input item-floating-label">
                    <span class="input-label">قیمت از</span>
                    <input type="text" placeholder="قیمت از" ng-model="channel.title" >
                </label>
                <label class="item item-input item-floating-label">
                    <span class="input-label">قیمت تا</span>
                    <input type="text" placeholder="قیمت تا" ng-model="channel.address" >
                </label>              

            </div>
            <div class="padding">
                <button type="submit" class="button button-block button-dark">جستجوی کالا</button>
            </div>
        </form>
    </ion-content>
</ion-view>
